services:
  - type: web
    runtime: static
    name: nofobo
    plan: free
    branch: master
    buildCommand: npm install && npx vite build
    staticPublishPath: ./web/dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://nofobo-api.deno.dev
